<h1>Categories</h1>

<ul>

  <% @categories.each do |c| %>
    <li><%=  link_to c.name, c%>(<%= c.soups.count%>)</li>
  <% end %>

  <% if session[:vip] %>
    <li>Secret soupo of the day</li>
  <% end %>

</ul>
